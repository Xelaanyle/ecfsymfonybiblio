{% extends 'base.html.twig' %}

{% block title %}Emprunteur{% endblock %}

{% block body %}
    <h1>Emprunteur</h1>

    <table >
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.emprunteur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ user.emprunteur.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ user.emprunteur.prenom }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ user.emprunteur.tel }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_profile_emprunteur_show', {'id': user.id}) }}">Retour</a>

    <a href="{{ path('app_profile_emprunteur_edit', {'id': user.id}) }}">Modifier</a>

{% endblock %}
